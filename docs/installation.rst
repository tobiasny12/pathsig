Installation
============

Requirements
------------

- Python 3.10+
- PyTorch >= 2.4 with CUDA support
- A CUDA toolkit compatible with your PyTorch build (only required for source installs)

Install
-------

.. code-block:: bash

   pip install pathsig

Installation is currently from source. Prebuilt wheels will be available soon.